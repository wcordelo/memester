import PublicationFeed from "../components/publication/PublicationFeed";
import { PageWrapper } from "../components/theme/Theme";

function Publication(): JSX.Element {
  return (
    <PageWrapper>
      <PublicationFeed />
    </PageWrapper>
  );
}

export default Publication;
